# iOS external frameworks

Place provider `.framework` or `.xcframework` bundles here, then link or embed them from the
project's `Zui.cmake` file according to the provider's distribution instructions.
